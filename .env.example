# Neon DB connection string for Prisma
DATABASE_URL="postgresql://user:password@endpoint.neon.tech/neondb?sslmode=require"

# Midnight Network Configuration
# Use 'preprod' or 'preview'
NEXT_PUBLIC_NETWORK_ID="preprod"

# Optional: The address of the deployed contract (if you want to hardcode a specific party for all users)
# Leave blank to allow users to input the contract address dynamically in the UI
NEXT_PUBLIC_CONTRACT_ADDRESS=""
